# Where the API lives, no trailing slash. Defaults to the production API when
# unset, so a local `next dev` works against real data out of the box.
NEXT_PUBLIC_API_BASE_URL=https://api-production-ce83.up.railway.app
